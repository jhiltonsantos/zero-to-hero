import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:rotas_loja/app/modules/payment/payment_page.dart';

void main() {
  testWidgets('PaymentPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PaymentPage(title: 'Payment')));
    //  final titleFinder = find.text('Payment');
    //  expect(titleFinder, findsOneWidget);
  });
}
